# Walkthrough: Neues Modal-Eingabesystem

Ich habe das veraltete und oft blockierte `window.prompt` durch ein modernes, UI-basiertes Eingabesystem ersetzt.

## Änderungen

### 1. Zentrales Eingabe-Modal
Ein neues `div#input-modal` am Ende der Seite dient als universelles Eingabefenster für alle Textaktionen. Es passt sich optisch an das Design der App an (inkl. Dark Mode support).

### 2. Refactored Funktionen
Folgende Aktionen nutzen nun das neue Modal:
*   **Neue Gruppe erstellen** (`+ Neu`)
*   **Gruppe umbenennen** (`✏️`)
*   **Item hinzufügen** (Klick auf leeren Slot bei Charakteren)

## Verifizierung

### Manuelles Testen
1.  Öffne den Tab **🛡️ Gruppe**.
2.  Klicke auf **+ Neu**. Statt eines Browser-Popups sollte nun ein schickes Overlay erscheinen.
3.  Gib einen Namen ein und bestätige mit Enter oder OK.
4.  Klicke auf **✏️**, ändere den Namen im Overlay.
5.  Klicke bei einem Helden auf einen leeren Slot (`+`). Das Overlay fragt nun nach dem Item-Namen.

## Screenshots
_Das neue Modal in Aktion:_
![Modal für neue Gruppe](/absolute/path/to/screenshot_modal.png)  
*(Hinweis: Screenshot muss noch erstellt/eingebunden werden, falls gewünscht)*
